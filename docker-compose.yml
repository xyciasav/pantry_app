services:
  pantry:
    build: .
    container_name: shelf-life
    restart: unless-stopped
    ports:
      - "${PANTRY_PORT:-8000}:8000"
    volumes:
      - pantry-data:/data
volumes:
  pantry-data:
